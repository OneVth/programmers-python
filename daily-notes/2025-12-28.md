# 2025-12-28 학습 노트

## 📝 풀이한 문제 (4문제)

| 문제번호 | 제목 | 핵심 개념 | 복습 |
|---------|------|----------|:----:|
| 181855 | 문자열 묶기 | Counter로 길이별 빈도 계산 | 🔁 |
| 181856 | 배열 비교하기 | 튜플 비교, cmp 패턴 `(a>b)-(a<b)` | 🔁 |
| 181857 | 배열의 길이를 2의 거듭제곱으로 만들기 | 비트 시프트, `bit_length()` | 🔁 |
| 181858 | 무작위로 K개의 수 뽑기 | 순서 유지 중복 제거, `dict.fromkeys()` | 🔁 |

---

## 🎯 핵심 학습 내용

### 1. Counter로 빈도 계산 (문제 #181855)

문자열 길이별로 그룹화할 때, 문자열 자체를 저장할 필요 없이 **개수만 세면** 된다:

```python
from collections import Counter

# 문자열 저장 (불필요한 메모리 사용)
temp = dict()
for s in strArr:
    temp.setdefault(len(s), []).append(s)
return max(map(len, temp.values()))

# 개수만 저장 (효율적)
counts = Counter(len(s) for s in strArr)
return max(counts.values())
```

**핵심:** 문제가 요구하는 것이 "개수"인지 "내용"인지 파악!

---

### 2. 튜플 비교와 cmp 패턴 (문제 #181856)

Python 튜플은 **사전순(lexicographic)** 비교를 수행한다:

```python
(3, 100) > (3, 50)   # True: 첫 요소 같음 → 두 번째로 결정
(2, 999) < (3, 1)    # True: 첫 요소에서 결정 (2 < 3)
```

**cmp 패턴 - Python 2의 cmp() 대체:**
```python
def cmp(a, b):
    return (a > b) - (a < b)
# a > b: True - False = 1 - 0 = 1
# a < b: False - True = 0 - 1 = -1
# a == b: False - False = 0 - 0 = 0
```

**문제 적용:**
```python
def solution(arr1, arr2):
    t1 = (len(arr1), sum(arr1))
    t2 = (len(arr2), sum(arr2))
    return (t1 > t2) - (t1 < t2)
```

→ 상세 내용: `notes/sequence-comparison.md` (cmp 패턴 섹션 추가됨)

---

### 3. 2의 거듭제곱 찾기 (문제 #181857)

배열 길이보다 크거나 같은 가장 작은 2의 거듭제곱을 찾는 두 가지 방법:

```python
# 방법 1: while + 비트 시프트
ex = 1
while ex < length:
    ex <<= 1  # 2배씩 증가

# 방법 2: bit_length() 활용
target = 1 << (length - 1).bit_length()
```

**`bit_length()` 동작:**
- `6.bit_length()` = 3 (이진수 110 → 3비트)
- `(6-1).bit_length()` = 3, `1 << 3` = 8 ✓
- `4.bit_length()` = 3, `(4-1).bit_length()` = 2, `1 << 2` = 4 ✓

**리스트 패딩 관용구:**
```python
arr + [0] * (target - len(arr))  # 뒤에 0으로 채우기
```

---

### 4. 순서 유지 중복 제거 (문제 #181858)

일반 `set()`은 순서를 보장하지 않는다. 순서를 유지하면서 중복을 제거하는 패턴:

```python
# 패턴 1: set + list 조합 (조기 종료 가능)
seen = set()
answer = []
for x in arr:
    if len(answer) >= k:
        break  # 효율적!
    if x not in seen:
        seen.add(x)
        answer.append(x)

# 패턴 2: dict.fromkeys() (Python 3.7+ 삽입 순서 보장)
unique = list(dict.fromkeys(arr))[:k]
```

**트레이드오프:**
| 상황 | set+list | dict.fromkeys |
|------|----------|---------------|
| arr 앞부분에서 k개 찾음 | ✅ 빠름 (조기 종료) | ❌ 전체 처리 |
| 코드 간결성 | 10줄 | 1줄 |

---

## 📚 업데이트한 학습 노트

- `notes/sequence-comparison.md` — cmp 패턴 섹션 추가
  - `(a > b) - (a < b)` 패턴 설명
  - 튜플 비교를 활용한 다중 조건 비교 예시

---

## 💡 오늘의 깨달음

1. **Counter 활용**: 빈도만 필요할 때 객체를 저장하지 말고 개수만 세자
2. **튜플 비교 매핑**: 문제의 비교 조건을 튜플 구조로 설계하면 한 줄 해결
3. **비트 연산**: 2의 거듭제곱 관련 문제는 `<<`, `bit_length()` 활용
4. **순서 유지 중복 제거**: `dict.fromkeys()`는 Pythonic하지만, 조기 종료가 필요하면 set+list
5. **패딩 패턴**: `arr + [value] * n`은 리스트 뒤에 값 채우는 관용구

---

## 🔄 다음에 복습할 것

- [ ] 181855 — Counter 활용, 불필요한 저장 피하기
- [ ] 181856 — cmp 패턴 `(a>b)-(a<b)`, 튜플 비교 활용
- [ ] 181857 — `bit_length()`로 2의 거듭제곱 계산
- [ ] 181858 — 순서 유지 중복 제거, dict.fromkeys vs set+list 트레이드오프
