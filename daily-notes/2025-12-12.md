# 2025-12-12 (금) 코딩테스트 일지

## 📝 오늘 활동: Lv0 문제 5개 + 새 개념 노트 작성

---

## 📝 오늘 푼 문제 (5문제)

| # | 문제 | 핵심 개념 | 복습 |
|---|------|-----------|:----:|
| 120871 | 저주의 숫자 3 | 시뮬레이션, 조건 필터링 | 🔁 |
| 120875 | 평행 | 기울기 비교, Fraction | 🔁 |
| 120876 | 겹치는 선분의 길이 | 구간 카운팅, 집합 연산, 오프셋 매핑 | 🔁 |
| 120878 | 유한소수 판별하기 | GCD, 기약분수, 소인수 판별 | |
| 120880 | 특이한 정렬 | **다중 키 정렬**, bisect 삽입 | 🔁 |

---

## 💡 오늘의 핵심 배움

### 1. 다중 키 정렬 (Multi-Key Sorting) ⭐ NEW

Python `sorted()`에서 튜플을 key로 반환하면 여러 기준으로 정렬 가능:

```python
# n과의 거리 오름차순 → 값 내림차순
sorted(numlist, key=lambda x: (abs(x - n), -x))
```

**원리**: 튜플은 사전순(lexicographic) 비교
- 첫 번째 요소로 1차 정렬
- 같으면 두 번째 요소로 2차 정렬
- 숫자 역순은 `-x`로 부호 반전

> 💾 `notes/multi-key-sorting.md` 신규 작성

### 2. 구간 카운팅 + 오프셋 매핑

좌표 범위가 음수를 포함할 때 **고정 오프셋**으로 인덱스 변환:

```python
# -100 ~ 100 → 0 ~ 200
overlap_num = [0] * 201

for start, end in lines:
    for i in range(start + 100, end + 100):
        overlap_num[i] += 1
```

**장점**: 원본 데이터 수정 없이 안전하게 처리

### 3. 집합 연산으로 겹치는 구간 찾기

```python
# (A∩B) ∪ (B∩C) ∪ (A∩C) = 2개 이상 겹치는 점들
sets = [set(range(start, end)) for start, end in lines]
len(sets[0] & sets[1] | sets[0] & sets[2] | sets[1] & sets[2])
```

### 4. 입력값 불변성 (Immutability) 주의

```python
# ❌ 원본 수정 - 부작용 발생 가능
for line in lines:
    line[0] += offset

# ✅ 오프셋만 적용 - 원본 유지
for start, end in lines:
    for i in range(start + offset, end + offset):
```

---

## ⚠️ 오늘 유의할 점

### 120871 — 저주의 숫자 3: 시뮬레이션 루프 구조

```python
# ❌ 헷갈리기 쉬운 구조
answer = 1
i = 1
while i <= n:
    while answer % 3 == 0 or "3" in str(answer):
        answer += 1
    i += 1
    answer += 1
return answer - 1  # 마지막에 -1 필요!

# ✅ 더 명확한 구조
answer = 0
for _ in range(n):
    answer += 1
    while is_cursed(answer):  # 저주받은 숫자면 건너뛰기
        answer += 1
return answer
```

**핵심**: "n번째 유효한 숫자 찾기" = 유효한 숫자를 n번 카운트

### 120875 — 평행: 선분 쌍 조합과 기울기 비교

```python
# 4개의 점으로 만들 수 있는 "겹치지 않는" 선분 쌍 = 3가지만!
# (0-1, 2-3), (0-2, 1-3), (0-3, 1-2)

# ❌ 기울기 비교 시 나눗셈 → 부동소수점 오차 위험
grad1 = (y2 - y1) / (x2 - x1)

# ✅ 교차 곱셈으로 정수 연산만 사용
# dy1/dx1 == dy2/dx2  ⟺  dy1 * dx2 == dy2 * dx1
(y1 - y2) * (x3 - x4) == (y3 - y4) * (x1 - x2)
```

**핵심**:
1. 4점에서 평행 선분 쌍은 **정확히 3가지** 조합만 확인
2. 기울기 비교는 **교차 곱셈**으로 나눗셈 회피

---

### 120876 — 원본 리스트 수정으로 인한 런타임 에러

```python
# ❌ 잘못된 코드 (프로그래머스에서 런타임 에러)
if left < 0:
    for line in lines:
        line[0] += -left  # 원본 수정!
        line[1] += -left

# ✅ 수정된 코드 (고정 오프셋 사용)
for start, end in lines:
    for i in range(start + 100, end + 100):  # 계산 시에만 오프셋 적용
```

**교훈**: 온라인 저지에서는 입력값 재사용/검증이 있을 수 있으므로 원본 수정 지양

---

## 📚 새로 작성한 문서

- `notes/multi-key-sorting.md` — 다중 키 정렬 완전 정리
  - 튜플 비교 원리
  - 역순 정렬 트릭 (숫자 `-x`, 문자열 처리법)
  - 자주 쓰는 패턴 모음
  - `operator.itemgetter` 활용법

---

## 🔄 다음에 복습할 것

### 체화 필요 (다음 주간 복습)
- [ ] 120871 — 저주의 숫자 3 (시뮬레이션, 조건 필터링)
- [ ] 120875 — 평행 (기울기 비교, Fraction)
- [ ] 120876 — 겹치는 선분의 길이 (구간 카운팅, 집합 연산)
- [ ] 120880 — 특이한 정렬 (다중 키 정렬)

---

## 📈 학습 통계

- **새 문제**: 5문제
- **새 개념**: 다중 키 정렬
- **주요 배움**: 튜플 key 활용, 입력값 불변성
