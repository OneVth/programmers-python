# 2025-12-05 (금) 코딩테스트 일지

## 📝 오늘 푼 문제 (16문제)

| # | 문제 | 핵심 개념 | 복습 |
|---|------|-----------|:----:|
| 120816 | 피자 나눠 먹기 (3) | 올림 나눗셈 `(n + k - 1) // k` | |
| 120817 | 배열의 평균값 | `sum() / len()`, `mean()` | |
| 120818 | 옷가게 할인 받기 | 조건 분기, 구간 할인율 | |
| 120819 | 아이스 아메리카노 | `divmod()` 활용 | |
| 120820 | 나이 출력 | 단순 수식 `2022 - age + 1` | |
| 120821 | 배열 뒤집기 | 슬라이싱 `[::-1]`, `reverse()` | |
| 120822 | 문자열 뒤집기 | 슬라이싱 `[::-1]`, `reversed()` | |
| 120823 | 직각삼각형 출력 기 | `*` 연산, `join()` | |
| 120824 | 짝수 홀수 개수 | 리스트 컴프리헨션 + 조건 | |
| 120825 | 문자 반복 출력하기 | 문자열 `*` 연산 | |
| 120826 | 특정 문자 제거하기 | `replace()`, 컴프리헨션 | |
| 120829 | 각도기 | 다중 조건 분기 | |
| 120830 | 양꼬치 | 서비스 로직 수식화 | |
| 120831 | 짝수의 합 | `range(2, n+1, 2)` step 활용 | |
| 120833 | 배열 자르기 | 슬라이싱 `[start:end+1]` | |
| 120834 | 외계행성의 나이 | `chr()`, `ord()`, `translate()` | 🔁 |

---

## 💡 오늘의 핵심 배움

### 1. str.translate / str.maketrans
```python
# 문자 변환 테이블 생성
table = str.maketrans("0123456789", "abcdefghij")
"234".translate(table)  # → "cde"
```
- **단일 문자끼리만** 매핑 가능 (multi-char 불가)

### 2. Python dict 삽입 순서 보장 (3.7+)
```python
d = {"a": 1, "b": 2, "c": 3}
list(d.keys())  # 항상 ["a", "b", "c"] 순서 보장
```

### 3. float vs Decimal
```python
0.1 + 0.2  # 0.30000000000000004 (부동소수점 오차)
Decimal("0.1") + Decimal("0.2")  # Decimal('0.3') (정확)
```

### 4. Mutable vs Immutable
```python
# ❌ Mutable 공유 참조 문제
a = [[]] * 3  # [[ref], [ref], [ref]] - 같은 리스트 참조!
a[0].append(1)  # [[1], [1], [1]]

# ✅ 각각 새로운 객체 생성
a = [[] for _ in range(3)]
```

### 5. 슬라이싱 심화
```python
s = "hello"
s[::2]   # "hlo" (step=2)
s[::-1]  # "olleh" (역순)
s[1:4:2] # "el" (start:stop:step)
```

---

## 📚 새로 작성한 문서
- `notes/str-translate-maketrans.md`
- `notes/dict-insertion-order.md`
- `notes/float-vs-decimal.md`
- `notes/mutable-vs-immutable.md`
- `notes/python-slicing.md`
- `notes/lcm-gcd-relationship.md`
- `.claude/commands/new-problem.md` (자동화 커맨드)

---

## 🔄 다음에 복습할 것
- [ ] translate vs replace 성능 비교
- [ ] Decimal 활용 금융 계산
- [ ] 슬라이싱 메모리 효율 (view vs copy)
