# 2025-12-09 (월) 코딩테스트 일지

## 📝 오늘 푼 문제 (4문제)

| # | 문제 | 핵심 개념 | 복습 |
|---|------|-----------|:----:|
| 120862 | 최댓값 만들기 (2) | 음수 곱, **정렬 양끝 비교** | |
| 120863 | 다항식 더하기 | 문자열 파싱, walrus `:=`, `replace()` | 🔁 |
| 120864 | 숨어있는 숫자의 덧셈 (2) | `re.findall(r'\d+')`, **연속 숫자 추출** | 🔁 |
| 120866 | 안전지대 | **Set 좌표 추적**, 8방향 탐색, `set.update()` | 🔁 |

---

## 💡 오늘의 핵심 배움

### 1. 음수를 포함한 최댓값 곱 문제
```python
# 음수 × 음수 = 양수 가능성!
numbers = [-5, -4, 1, 2, 3]

# 최댓값 후보: 최솟값 두 개 곱 vs 최댓값 두 개 곱
sorted_nums = sorted(numbers)
max(
    sorted_nums[0] * sorted_nums[1],    # (-5) × (-4) = 20 ✓
    sorted_nums[-1] * sorted_nums[-2]   # 3 × 2 = 6
)
```
- 양수만 있으면 → 가장 큰 두 수 곱
- 음수가 있으면 → 가장 작은 두 수(음수)의 곱도 확인!

### 2. 다항식 파싱 패턴
```python
polynomial = "3x + 7 + x + 2"
tokens = polynomial.split(" + ")  # ['3x', '7', 'x', '2']

coefficient = 0  # x의 계수
constant = 0     # 상수

for token in tokens:
    if "x" in token:
        # 'x' → 1, '3x' → 3
        coefficient += 1 if token == "x" else int(token[:-1])
    else:
        constant += int(token)

# 결과 포맷팅 (계수 1 생략, 0 처리)
if coefficient == 0:
    result = f"{constant}"
elif constant == 0:
    result = "x" if coefficient == 1 else f"{coefficient}x"
else:
    result = f"{'x' if coefficient == 1 else f'{coefficient}x'} + {constant}"
```

### 3. 연속 숫자 추출 (re.findall)
```python
import re

my_string = "aAb1B2cC34oOp"

# 방법 1: 정규표현식 (가장 간결)
nums = re.findall(r'\d+', my_string)  # ['1', '2', '34']
total = sum(int(n) for n in nums)     # 37

# 방법 2: 문자→공백 치환 후 split
s = ''.join(c if c.isdigit() else ' ' for c in my_string)
# '   1 2  34   '
total = sum(int(n) for n in s.split())  # 37

# 방법 3: 순회하며 누적 (O(1) 공간)
temp, total = "", 0
for c in my_string + "x":  # 끝에 문자 추가로 마지막 숫자 처리
    if c.isdigit():
        temp += c
    elif temp:
        total += int(temp)
        temp = ""
```

### 4. Set으로 좌표 추적 (2D 그리드)
```python
# 지뢰(1) 주변 8방향 + 자신을 위험지역으로 마킹
danger = set()
for i, row in enumerate(board):
    for j, x in enumerate(row):
        if x == 1:  # 지뢰 발견
            # 9칸(자신+8방향)의 좌표를 한 번에 추가
            danger.update(
                (i + di, j + dj)
                for di in [-1, 0, 1]
                for dj in [-1, 0, 1]
            )

# 경계 내 위험 좌표만 카운트
n = len(board)
valid_danger = sum(0 <= i < n and 0 <= j < n for i, j in danger)
safe_count = n * n - valid_danger
```
- **Set 장점**: 중복 자동 제거, O(1) 멤버십 테스트
- **`set.update()`**: 여러 요소를 한 번에 추가

### 5. 8방향 탐색 패턴 비교
```python
# 방법 1: 명시적 딕셔너리 (가독성 좋음)
direction = {
    "e": (1, 0), "w": (-1, 0), "s": (0, -1), "n": (0, 1),
    "ne": (1, 1), "nw": (-1, 1), "se": (1, -1), "sw": (-1, -1),
}
for dx, dy in direction.values():
    nx, ny = x + dx, y + dy

# 방법 2: 중첩 루프 (간결함)
for di in [-1, 0, 1]:
    for dj in [-1, 0, 1]:
        ni, nj = i + di, j + dj

# 방법 3: itertools.product (함수형)
from itertools import product
for di, dj in product([-1, 0, 1], repeat=2):
    if di == dj == 0:  # 자기 자신 제외시
        continue
```

---

## 📚 새로 작성한 문서
- `notes/set-operations.md` (Set 완벽 가이드)

---

## ⚠️ 접근법 비교

### 안전지대 문제 - 3가지 접근법

| 접근법 | 공간 복잡도 | 특징 |
|--------|-------------|------|
| v1: safe_mat 배열 | O(n²) | 직관적, 별도 배열 생성 |
| v2: **set 좌표** | O(k) | 중복 자동 제거, k=위험좌표 수 |
| v3: in-place 마킹 | O(1) | 원본 수정 (부작용 있음) |

```python
# v2가 가장 효율적 (지뢰가 적을 때)
# - 지뢰가 많으면 v1이 나을 수 있음
# - v3는 원본 훼손 주의!
```

---

## 🔄 다음에 복습할 것
- [ ] Set 집합 연산 (`|`, `&`, `-`, `^`) 실전 활용
- [ ] frozenset 사용 케이스 (dict key, set of sets)
- [ ] BFS/DFS에서 visited를 set으로 관리
- [ ] 다항식 문제 확장 (곱셈, 미분 등)
