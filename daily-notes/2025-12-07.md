# 2025-12-07 (일) 코딩테스트 일지

## 📝 오늘 푼 문제 (6문제)

| # | 문제 | 핵심 개념 | 복습 |
|---|------|-----------|:----:|
| 120843 | 공 던지기 | 원형 배열, 모듈러 연산 `% len` | |
| 120844 | 배열 회전시키기 | `deque.rotate()`, 슬라이싱 | |
| 120845 | 주사위의 개수 | `math.prod()`, reduce | |
| 120846 | 합성수 찾기 | **에라토스테네스의 체**, 소수 판별 | 🔁 |
| 120847 | 최댓값 만들기 (1) | `heapq.nlargest()` | |
| 120848 | 팩토리얼 | **bisect** 이분탐색, 테이블 활용 | 🔁 |

---

## 💡 오늘의 핵심 배움

### 1. 에라토스테네스의 체
```python
def sieve(n):
    is_prime = [True] * (n + 1)
    is_prime[0] = is_prime[1] = False

    for i in range(2, int(n**0.5) + 1):
        if is_prime[i]:
            for j in range(i*i, n+1, i):  # i*i부터!
                is_prime[j] = False
    return is_prime
```
- **O(n log log n)** - n 이하 모든 소수를 효율적으로
- `i*i`부터 시작하는 이유: 더 작은 배수는 이미 제거됨

### 2. heapq 모듈
```python
from heapq import nlargest, nsmallest

nlargest(2, [5, 3, 8, 1])   # [8, 5]
nsmallest(2, [5, 3, 8, 1])  # [1, 3]
```
- **k가 작을 때** 정렬보다 효율적: O(n + k log n) vs O(n log n)
- 우선순위 큐, Top K 문제에 활용

### 3. bisect 모듈 (이분탐색)
```python
from bisect import bisect_left, bisect_right

nums = [1, 3, 3, 3, 5]
bisect_left(nums, 3)   # 1 (3이 시작되는 위치)
bisect_right(nums, 3)  # 4 (3이 끝나는 다음 위치)
```
- **left**: 같은 값의 왼쪽 (첫 등장)
- **right**: 같은 값의 오른쪽 (마지막 다음)

### 4. reduce vs 전용 함수
```python
from functools import reduce
from math import prod

# ❌ reduce (불필요하게 복잡)
reduce(lambda a, b: a * b, lst)

# ✅ 전용 함수 (간결하고 빠름)
prod(lst)
```
- sum, prod, max, min 있으면 그걸 쓰자!
- reduce는 **커스텀 누적 연산**에만

### 5. 원형 배열 인덱싱
```python
# index % len 으로 순환 처리
def get_circular(arr, i):
    return arr[i % len(arr)]
```

---

## 📚 새로 작성한 문서
- `notes/heapq.md` (힙 자료구조 종합)
- `notes/bisect.md` (이분탐색 모듈)
- `notes/sieve-of-eratosthenes.md` (에라토스테네스의 체)
- `notes/filter-map-reduce.md` (reduce/aggregate 추가)

---

## ⚠️ 새로 배운 개념

### DP vs 단순 점화식
```
팩토리얼: f(i) = f(i-1) * i
→ 점화식은 있지만, 중복 계산이 없어서 진정한 DP는 아님

진짜 DP: 선택/최적화가 있고, 중복 부분 문제가 있음
예) 배낭 문제, 동전 교환, 최단 경로
```

---

## 🔄 다음에 복습할 것
- [ ] 에라토스테네스 변형 (최소 소인수 저장)
- [ ] heapq로 중앙값 찾기 (두 힙 활용)
- [ ] bisect로 lower_bound/upper_bound 구현
- [ ] 다익스트라 알고리즘 (heapq 활용)
