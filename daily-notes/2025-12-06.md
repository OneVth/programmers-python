# 2025-12-06 (토) 코딩테스트 일지

## 📝 오늘 푼 문제 (8문제)

| # | 문제 | 핵심 개념 | 복습 |
|---|------|-----------|:----:|
| 120835 | 진료 순서 정하기 | `sorted()` + `index()`, 랭킹 | |
| 120836 | 순서쌍의 개수 | 약수 개수 세기, √n 최적화 | |
| 120837 | 개미 군단 | **그리디 vs DP**, 동전 교환 | 🔁 |
| 120838 | 모스부호 (1) | dict 매핑, `split()` + `join()` | |
| 120839 | 가위 바위 보 | `translate()` (단일 문자만!) | 🔁 |
| 120840 | 구슬을 나누는 경우의 수 | `math.comb()`, 팩토리얼 | |
| 120841 | 점의 위치 구하기 | 사분면, 수학 공식 트릭 | |
| 120842 | 2차원으로 만들기 | 슬라이싱 + step, `reshape` | |

---

## 💡 오늘의 핵심 배움

### 1. DP vs 그리디 관계
```
그리디: 매 순간 최선의 선택 (탐욕)
DP: 작은 문제 → 큰 문제 (점화식)

그리디 적용 가능 = DP로도 해결 가능 ✅
DP 문제 = 반드시 그리디 가능? ❌
```

### 2. DP 5단계 프레임워크
1. **문제 유형 파악** (최적화/카운팅)
2. **상태 정의** (dp[i]가 뭘 의미?)
3. **점화식 도출** (dp[i] = ?)
4. **베이스 케이스** (dp[0] = ?)
5. **정답 추출** (dp[n]? max(dp)?)

### 3. str.translate 제한사항
```python
# ✅ 단일 문자 → 단일 문자 (가능)
"025".translate(str.maketrans("025", "502"))

# ❌ 다중 문자 key (불가능!)
morse_to_alpha = {".-": "a", "-...": "b"}  # translate 불가
```

### 4. 리스트 초기화 주의
```python
# ❌ 참조 공유 (2D 배열에서 위험!)
arr = [[0] * 3] * 3  # 같은 리스트 3개 참조

# ✅ 각각 독립 객체
arr = [[0] * 3 for _ in range(3)]
```

### 5. 수학 공식으로 사분면 계산
```python
# Boolean을 인덱스로 활용
quad = [[3, 2], [4, 1]]
return quad[x > 0][y > 0]
```

---

## 📚 새로 작성한 문서
- `notes/dp-greedy.md` (DP/그리디 종합 가이드)
- `notes/list-initialization.md` (리스트 초기화 패턴)
- `notes/filter-map-reduce.md` (함수형 프로그래밍)
- `notes/str-translate-maketrans.md` (key 제한 추가)

---

## ⚠️ 실수한 점

### translate로 모스부호 해독 시도
```python
# 모스부호는 multi-char key (".-", "-...")
# translate는 single-char만 지원 → ValueError!
```
- **교훈**: 도구의 제한사항을 먼저 확인하자

---

## 🔄 다음에 복습할 것
- [ ] DP 문제 유형별 연습 (배낭, LCS, 동전)
- [ ] 그리디 적용 가능 조건 (증명 방법)
- [ ] 점화식 도출 연습
