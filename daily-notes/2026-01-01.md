# 2026-01-01 학습 노트

## 🔁 복습한 문제 (10문제)

| 문제번호 | 제목 | 핵심 개념 | 복습 차수 | 재복습 |
|---------|------|----------|:---------:|:------:|
| 181844 | 배열의 원소 삭제하기 | Set 변환 O(1) 조회 | 1차 | |
| 181847 | 0 떼기 | `lstrip()` | 1차 | |
| 181853 | 뒤에서 5등까지 | `sorted()[:k]`, `heapq.nsmallest()` | 1차 | 🔄 |
| 181855 | 문자열 묶기 | `Counter` 길이별 카운트 | 1차 | 🔄 |
| 181856 | 배열 비교하기 | cmp 패턴, 튜플 비교 | 1차 | 🔄 |
| 181857 | 배열의 길이를 2의 거듭제곱으로 만들기 | 비트 시프트, `bit_length()` | 1차 | |
| 181858 | 무작위로 K개의 수 뽑기 | `dict.fromkeys()` 순서 유지 중복 제거 | 1차 | |
| 181861 | 배열의 원소만큼 추가하기 | `+=` extend, `chain.from_iterable()` | 1차 | 🔄 |
| 181862 | 세 개의 구분자 | `re.split()` 다중 구분자 | 1차 | 🔄 |
| 181865 | 간단한 식 계산하기 | `operator` 모듈 | 1차 | |

---

## 🎯 핵심 학습 내용

### 1. cmp 패턴 - 비교 결과를 -1, 0, 1로 반환 (문제 #181856)

**문제:** 두 배열의 대소관계를 비교하여 -1, 0, 1 반환

**패턴:**
```python
# Python 2의 cmp() 대체 패턴
(a > b) - (a < b)

# 동작 원리:
# a > b → True - False = 1 - 0 = 1
# a < b → False - True = 0 - 1 = -1
# a == b → False - False = 0 - 0 = 0
```

**활용 예시:**
```python
def solution_v1(arr1, arr2):
    if len(arr1) != len(arr2):
        return (len(arr1) > len(arr2)) - (len(arr1) < len(arr2))
    return (sum(arr1) > sum(arr2)) - (sum(arr1) < sum(arr2))
```

---

### 2. 튜플 비교 - 다중 기준 정렬 (문제 #181856)

**핵심:** Python 튜플은 사전순(lexicographic) 비교

```python
# 첫 번째 요소가 다르면 첫 번째로 결정
(3, 10) > (2, 999)  # True (3 > 2)

# 첫 번째가 같으면 두 번째로 결정
(3, 10) > (3, 5)    # True (10 > 5)

# 활용: "길이 우선, 같으면 합 비교"
def solution_v2(arr1, arr2):
    t1, t2 = (len(arr1), sum(arr1)), (len(arr2), sum(arr2))
    return (t1 > t2) - (t1 < t2)
```

---

### 3. Set 조회 최적화 (문제 #181844)

**문제:** arr에서 delete_list에 있는 원소 모두 삭제

```python
# ❌ 비효율적: O(n × m)
[x for x in arr if x not in delete_list]

# ✅ 최적화: O(n + m)
delete_set = set(delete_list)
[x for x in arr if x not in delete_set]
```

**원리:** 리스트 `in` 연산 O(n) → Set `in` 연산 O(1)

---

### 4. heapq.nsmallest() vs sorted() (문제 #181853)

**문제:** 가장 작은 5개의 수를 오름차순으로 반환

| 방식 | Time | Space | 언제 유리? |
|------|------|-------|----------|
| `sorted(lst)[:k]` | O(n log n) | O(n) | n이 작거나 k ≈ n |
| `heapq.nsmallest(k, lst)` | O(n log k) | O(k) | k << n |

```python
# 방법 1: 정렬 후 슬라이싱
sorted(num_list)[:5]

# 방법 2: heapq (대규모 데이터에 효율적)
import heapq
heapq.nsmallest(5, num_list)
```

---

### 5. Counter - 빈도 카운팅 (문제 #181855)

**문제:** 문자열 길이별 그룹 중 가장 큰 그룹의 크기

```python
# ❌ 비효율적: 문자열 자체를 저장 (O(n) 공간)
answer = {}
for s in strArr:
    answer.setdefault(len(s), []).append(s)
max(map(len, answer.values()))

# ✅ 효율적: 개수만 저장 (O(k) 공간, k=서로 다른 길이)
from collections import Counter
max(Counter(len(s) for s in strArr).values())
```

---

### 6. bit_length() - 2의 거듭제곱 계산 (문제 #181857)

**문제:** 배열 길이를 2의 거듭제곱으로 맞추기

```python
# 방법 1: while 루프
i = 1
while i < len(arr):
    i <<= 1  # i *= 2

# 방법 2: bit_length() 활용
target = 1 << (len(arr) - 1).bit_length() if len(arr) > 1 else 1

# bit_length() 이해:
# (1).bit_length() = 1  → 0b1
# (5).bit_length() = 3  → 0b101
# (8).bit_length() = 4  → 0b1000

# (n-1).bit_length()를 쓰는 이유:
# n=4 → (3).bit_length()=2 → 1<<2=4 ✓ (이미 2의 거듭제곱)
# n=5 → (4).bit_length()=3 → 1<<3=8 ✓
```

---

### 7. dict.fromkeys() - 순서 유지 중복 제거 (문제 #181858)

**문제:** 배열에서 중복 제거하되 순서 유지

```python
# ❌ set()은 순서 보장 안 함
list(set(arr))  # 순서 무작위

# ✅ dict.fromkeys()는 삽입 순서 보장 (Python 3.7+)
list(dict.fromkeys(arr))

# 동작 원리:
arr = [3, 1, 2, 1, 3]
dict.fromkeys(arr)  # {3: None, 1: None, 2: None}
list(...)           # [3, 1, 2]
```

---

### 8. chain.from_iterable() - 리스트 평탄화 (문제 #181861)

**문제:** 각 원소를 원소 값만큼 반복하여 하나의 리스트로

```python
# 방법 1: for 루프 + extend
answer = []
for i in arr:
    answer += [i] * i

# 방법 2: itertools.chain (함수형)
from itertools import chain
list(chain.from_iterable([i] * i for i in arr))

# ❌ 비효율적: sum()으로 평탄화 (O(n²))
sum([[i] * i for i in arr], [])
```

**chain vs chain.from_iterable:**
```python
chain(*[[1,1], [2,2]])         # 언패킹 필요
chain.from_iterable([[1,1], [2,2]])  # 제너레이터 직접 사용 (더 효율적)
```

---

### 9. re.split() - 다중 구분자 분할 (문제 #181862)

**문제:** a, b, c를 구분자로 문자열 분할

```python
# str.split()은 단일 구분자만 지원
"abc".split("a")  # ['', 'bc']

# re.split()은 정규식으로 다중 구분자 지원
import re
re.split(r"[abc]", "baconlettucetomato")
# → ['', '', 'onlettu', 'etom', 'to']

# 빈 문자열 필터링 필수!
[s for s in re.split(r"[abc]", myStr) if s]
```

**대안 - replace 체이닝:**
```python
for c in "bc":
    myStr = myStr.replace(c, "a")
myStr.split("a")
```

---

### 10. operator 모듈 - 연산자를 함수로 (문제 #181865)

**문제:** "a op b" 형태의 문자열 계산

```python
import operator

op_dict = {
    "+": operator.add,
    "-": operator.sub,
    "*": operator.mul,
    "/": operator.truediv,
    "//": operator.floordiv,
}

a, op, b = "43 + 12".split()
op_dict[op](int(a), int(b))  # 55
```

**eval() vs operator:**
```python
# eval()은 간단하지만 보안 위험!
eval("43 + 12")  # 55 ✓
eval("__import__('os').system('rm -rf /')")  # 💀

# operator는 안전하고 명시적
op_dict[op](int(a), int(b))  # 허용된 연산만 실행
```

---

## 📊 패턴 요약

| 패턴 | 용도 | 코드 |
|------|------|------|
| cmp 패턴 | 비교 결과 -1,0,1 | `(a > b) - (a < b)` |
| 튜플 비교 | 다중 기준 정렬 | `(len(x), sum(x))` |
| Set 조회 | O(1) 멤버십 | `set(list)` |
| Counter | 빈도 카운트 | `Counter(iterable)` |
| heapq | Top-K | `nsmallest(k, lst)` |
| dict.fromkeys | 순서 유지 중복 제거 | `list(dict.fromkeys(arr))` |
| bit_length | 비트 수 계산 | `n.bit_length()` |
| chain | 평탄화 | `chain.from_iterable(...)` |
| re.split | 다중 구분자 | `re.split(r"[...]", s)` |
| operator | 연산자 함수화 | `operator.add(a, b)` |

---

## 💡 오늘의 깨달음

1. **cmp 패턴 + 튜플 비교**: 다중 기준 비교를 한 줄로 해결
2. **Set 변환은 습관**: 존재 여부 확인이 반복되면 무조건 Set
3. **Counter는 개수만 필요할 때**: 원본 저장은 메모리 낭비
4. **bit_length()**: 2의 거듭제곱 관련 문제에서 유용
5. **dict.fromkeys()**: 순서 유지 중복 제거의 정석
6. **operator 모듈**: eval() 대신 안전한 연산자 처리
