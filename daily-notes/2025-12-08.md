# 2025-12-08 (일) 코딩테스트 일지

## 📝 오늘 푼 문제 (8문제)

| # | 문제 | 핵심 개념 | 복습 |
|---|------|-----------|:----:|
| 120849 | 모음 제거 | `str.translate()`, **정규표현식** | 🔁 |
| 120850 | 문자열 정렬하기 1 | `isdigit()`, `re.findall()` | |
| 120851 | 숨어있는 숫자의 덧셈 1 | **Generator Expression** O(1) 공간 | 🔁 |
| 120852 | 소인수분해 | 소인수분해 알고리즘, √n 최적화 | |
| 120853 | 컨트롤 제트 | Stack, **Walrus Operator** `:=` | 🔁 |
| 120854 | 배열 원소의 길이 | `map(len, arr)` | |
| 120860 | 직사각형 넓이 구하기 | **`zip(*)`** 좌표 분리 | 🔁 |
| 120861 | 캐릭터의 좌표 | 방향 매핑 딕셔너리, 클램핑 | |

---

## 💡 오늘의 핵심 배움

### 1. 정규표현식 (re 모듈)
```python
import re

# 패턴 매칭
re.findall(r'\d+', "a1b2c3")     # ['1', '2', '3']
re.sub(r'[aeiou]', '', "hello")  # 'hll'

# 자주 쓰는 패턴
r'\d'      # 숫자 [0-9]
r'\D'      # 숫자 아닌 것
r'[aeiou]' # 모음
r'\s'      # 공백
```
- **`re.findall`**: 모든 매칭 리스트로 반환
- **`re.sub`**: 치환 (translate보다 유연)

### 2. Generator Expression (Space O(1))
```python
# List Comprehension: O(n) 공간
sum([x for x in range(1000)])  # 리스트 전체 생성

# Generator Expression: O(1) 공간
sum(x for x in range(1000))    # 한 번에 하나씩만
```
- 괄호 `()` 대신 대괄호 `[]` 없이 사용
- **Lazy Evaluation**: 값을 필요할 때만 생성
- `sum()`, `max()`, `min()`, `any()`, `all()` 등과 함께 사용

### 3. Walrus Operator `:=` (Python 3.8+)
```python
# 할당과 동시에 값 반환
if (n := len(data)) > 10:
    print(f"길이 {n}은 너무 김")

# while에서 활용
while (line := input()) != "quit":
    print(line)

# 리스트 컴프리헨션에서
[y for x in data if (y := expensive(x)) > 0]
```
- **Assignment Expression**: 할당하면서 그 값을 표현식으로 사용
- 반복 계산 방지, 코드 간결화

### 4. zip과 zip(*) 언패킹
```python
# 병렬 순회
for a, b in zip([1,2], ['x','y']):
    print(a, b)  # 1 x, 2 y

# 좌표 분리 (전치)
points = [[1, 10], [2, 20], [3, 30]]
xs, ys = zip(*points)
# xs = (1, 2, 3), ys = (10, 20, 30)

# 직사각형 넓이
(max(xs) - min(xs)) * (max(ys) - min(ys))
```
- **`zip(*matrix)`**: 행렬 전치, 좌표 분리에 필수!

### 5. 방향 매핑 + 클램핑 패턴
```python
# 방향 매핑 (게임/시뮬레이션 필수)
moves = {
    "up": (0, 1), "down": (0, -1),
    "left": (-1, 0), "right": (1, 0)
}

# 클램핑 (범위 제한)
x = max(x - 1, -limit)  # 하한 클램핑
x = min(x + 1, limit)   # 상한 클램핑
```

---

## 📚 새로 작성한 문서
- `notes/regex.md` (정규표현식 종합)
- `notes/generator-expression.md` (Generator vs List Comprehension)
- `notes/walrus-operator.md` (할당 표현식)
- `notes/zip.md` (zip 함수와 언패킹)

---

## ⚠️ 접근법 비교

### 문자열에서 특정 문자 제거
```python
# 1. 리스트 컴프리헨션 (기본)
''.join(c for c in s if c not in 'aeiou')

# 2. translate (가장 빠름)
s.translate(str.maketrans('', '', 'aeiouAEIOU'))

# 3. 정규표현식 (유연함)
re.sub(r'[aeiou]', '', s)
```

### 경계 체크 방식
```
v1: 이동 전 체크 → if 가능하면 이동
v2: 이동 후 클램핑 → 이동 → if 초과시 제한
v3: min/max 인라인 → 이동과 제한을 한 줄로
```

---

## 🔄 다음에 복습할 것
- [ ] 정규표현식 그룹 캡처 `()`, lookahead/lookbehind
- [ ] Generator 직접 만들기 (yield)
- [ ] itertools.zip_longest (길이 다른 경우)
- [ ] 8방향 BFS/DFS에서 방향 매핑 활용
