# 2025-12-21 학습 노트

## 오늘 복습한 문제

| 번호 | 제목 | 핵심 개념 | 상태 |
|------|------|----------|:----:|
| 120880 | 특이한 정렬 | **튜플 키 정렬** | ✅ |
| 120883 | 로그인 성공? | dict 변환, **Walrus 연산자** | ✅ |
| 120885 | 이진수 더하기 | 진법 변환, `format(n, 'b')` | ✅ |
| 120886 | A로 B 만들기 | **Counter로 Anagram 판별** | 🔄 |
| 120888 | 중복된 문자 제거 | **dict.fromkeys()**, set+list | 🔄 |
| 120891 | 369게임 | 문자열 멤버십 `in` | 🔄 |

> 🔄 = 추가 복습 필요

## 복습 대기 중인 문제 (9개 + 3개 재복습)

| 번호 | 제목 | 핵심 개념 | 비고 |
|------|------|----------|------|
| 120886 | A로 B 만들기 | Counter, Anagram | 재복습 |
| 120888 | 중복된 문자 제거 | dict.fromkeys() | 재복습 |
| 120891 | 369게임 | 문자열 멤버십 | 재복습 |
| 120893 | 대문자와 소문자 | swapcase() | |
| 120897 | 약수 구하기 | 약수 열거 | |
| 120902 | 문자열 계산하기 | eval() 또는 파싱 | |
| 120904 | 숫자 찾기 | find(), 인덱스 | |
| 120905 | n의 배수 고르기 | 필터링 | |
| 120921 | 문자열 밀기 | **S+S 트릭** | |
| 120922 | 종이 자르기 | 수학 공식 `M*N-1` | |
| 120956 | 옹알이 (1) | **정규표현식** | |
| 181829 | 이차원 배열 대각선 순회 | **대각선 인덱스** | |

## 오늘 복습한 핵심 개념

### 1. 튜플 키를 이용한 다중 조건 정렬 (120880)

```python
# n과의 거리 오름차순, 같으면 값 내림차순
sorted(numlist, key=lambda x: (abs(x - n), -x))
```

**원리:** 튜플은 사전식(lexicographic) 비교 → 첫 번째 요소가 같을 때만 두 번째 비교

### 2. Walrus 연산자 `:=` (120883)

```python
db_dict = dict(db)
if pw := db_dict.get(id_pw[0]):  # 조회 + 할당 + 조건 검사
    if pw == id_pw[1]:
        return "login"
    return "wrong pw"
return "fail"
```

**주의:** `get()`이 `""`나 `0`을 반환하면 `False`가 됨 (falsy 값 주의)

### 3. 진법 변환 함수들 (120885)

| 방식 | 결과 | 후처리 |
|------|------|--------|
| `bin(10)` | `'0b1010'` | `[2:]` 필요 |
| `format(10, 'b')` | `'1010'` | 불필요 ✨ |
| `f"{10:b}"` | `'1010'` | 불필요 |

### 4. Counter vs sorted - Anagram 판별 (120886) 🔄

```python
# 방법 1: 정렬 비교 - O(n log n)
sorted(before) == sorted(after)

# 방법 2: Counter 비교 - O(n) ✨
from collections import Counter
Counter(before) == Counter(after)
```

**Counter가 O(n)인 이유:** dict 상속 → 해시 테이블 기반 → 조회/삽입 O(1)

### 5. 순서 유지 중복 제거 (120888) 🔄

```python
# v1: 리스트 in 검사 - O(n²)
answer = []
for c in my_string:
    if c not in answer:
        answer.append(c)

# v2: dict.fromkeys() - O(n) ✨ (Python 3.7+)
"".join(dict.fromkeys(my_string))

# v3: set + list 분리 - O(n)
seen = set()
answer = []
for c in my_string:
    if c not in seen:
        seen.add(c)
        answer.append(c)
```

### 6. 문자열 멤버십 검사 (120891) 🔄

```python
# ❌ 버그: 0 % 3 == 0도 True
sum(1 for c in str(order) if int(c) % 3 == 0)

# ✅ 정확: 3, 6, 9만 매칭
sum(c in "369" for c in str(order))
```

## 학습 통계

- 복습 완료: **3문제** (120880, 120883, 120885)
- 재복습 필요: **3문제** (120886, 120888, 120891)
- 복습 대기: **9문제**

## 내일 할 일

- [ ] 120886, 120888, 120891 재복습 (우선)
- [ ] 120893 ~ 120905 복습 (기초 문제)
- [ ] 120921, 120922, 120956, 181829 복습 (핵심 패턴)
