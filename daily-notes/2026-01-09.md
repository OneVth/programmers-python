# 2026-01-09 학습 노트

## 🔁 2차 복습 완료 (5문제)

| 문제번호 | 제목 | 핵심 개념 | 복습 차수 | 결과 |
|---------|------|----------|:---------:|:----:|
| 120864 | 숨어있는 숫자의 덧셈 (2) | re.split vs re.findall | 2차 | ✅ |
| 120866 | 안전지대 | Set 좌표 추적, 8방향 탐색 | 2차 | ✅ |
| 120886 | A로 B 만들기 | 애너그램 판별 (sorted/Counter) | 2차 | ✅ |
| 120888 | 중복된 문자 제거 | 순서 보존 중복 제거 | 2차 | ✅ |
| 120891 | 369게임 | 문자열 in 연산 | 2차 | ✅ |

---

## 🎯 핵심 학습 내용

### 1. 정규표현식 패턴 비교 (문제 #120864)

**문제:** 문자열에서 연속된 숫자들의 합 구하기

```python
# v1: split 방식 - 비숫자로 분리
return sum(int(s) for s in re.split(r'\D', my_string) if s)

# v2: findall 방식 - 숫자 직접 추출 (더 직관적)
return sum(int(s) for s in re.findall(r'\d+', my_string))
```

**핵심 인사이트:**
- `re.split(r'\D', s)` → 빈 문자열 포함되므로 `if s` 필터 필요
- `re.findall(r'\d+', s)` → 매칭된 것만 반환, 더 간결
- 의도가 "숫자 찾기"이므로 `findall`이 의미적으로 명확

---

### 2. Set 기반 위험지역 추적 (문제 #120866)

**문제:** 지뢰 주변 8방향 + 자신을 위험지역으로, 안전지역 수 계산

```python
def solution(board: list[list[int]]) -> int:
    n = len(board)
    danger = set()
    directions = [
        (-1, -1), (-1, 0), (-1, 1),
        (0, -1),  (0, 0),  (0, 1),  # (0,0) 포함이 핵심!
        (1, -1),  (1, 0),  (1, 1),
    ]
    for i in range(n):
        for j in range(n):
            if board[i][j]:
                danger.update((i + dy, j + dx) for dy, dx in directions)
    return n**2 - sum(1 for y, x in danger if 0 <= y < n and 0 <= x < n)
```

**핵심 인사이트:**
- **역발상**: 안전지역 세기 → 위험지역 세고 전체에서 차감
- `(0, 0)` 포함으로 지뢰 자체도 위험지역에 포함
- `set`으로 중복 좌표 자동 제거
- 범위 체크는 마지막에 한 번만

---

### 3. 애너그램 판별 패턴 (문제 #120886)

**문제:** 문자열 재배열로 다른 문자열을 만들 수 있는지 확인

```python
# v1: 정렬 비교 - O(n log n)
return int(sorted(before) == sorted(after))

# v2: Counter 비교 - O(n)
return int(Counter(before) == Counter(after))
```

**핵심 인사이트:**
- 두 방식 모두 정확하지만 시간복잡도가 다름
- n < 1000 수준에서는 실제 성능 차이 미미
- `sorted`는 import 불필요, `Counter`는 이론적 우위

---

### 4. 순서 보존 중복 제거 (문제 #120888)

**문제:** 문자열에서 중복 문자 제거, 첫 등장 순서 유지

```python
# v1: 명시적 set + list
def solution(my_string: str) -> str:
    answer, seen = [], set()
    for c in my_string:
        if c not in seen:
            answer.append(c)
            seen.add(c)
    return "".join(answer)

# v2: dict.fromkeys() 활용 (Python 3.7+)
return "".join(dict.fromkeys(my_string))
```

**핵심 인사이트:**
- `set`만으로는 순서 보장 안 됨 → 별도 리스트 필요
- Python 3.7+에서 dict는 삽입 순서 보존
- 면접에서는 v1이 설명하기 좋음, 실무에서는 v2가 간결

---

### 5. Boolean 합산 패턴 (문제 #120891)

**문제:** 숫자에서 3, 6, 9 개수 세기

```python
# v1: Counter 사용
counter = Counter(str(order))
return sum(counter[c] for c in "369")

# v2: in 연산 직접 사용 (더 효율적)
return sum(c in "369" for c in str(order))
```

**핵심 인사이트:**
- `c in "369"`는 boolean이지만 `sum()`에서 `True=1, False=0`
- v2는 O(1) 추가 공간, v1은 O(d) 공간 (d=자릿수)
- 길이 3 문자열 검색은 상수 시간

---

## 📊 복습 진행 현황

### 월간 복습 (16문제 중 11문제 완료)

| 상태 | 문제번호 | 제목 | 핵심 개념 |
|:----:|---------|------|----------|
| ✅ | 120812 | 최빈값 구하기 | Counter.most_common() |
| ✅ | 120834 | 외계행성의 나이 | translate + maketrans |
| ✅ | 120837 | 개미 군단 | 그리디 vs DP |
| ✅ | 120846 | 합성수 찾기 | 에라토스테네스의 체 |
| ✅ | 120848 | 팩토리얼 | 점진적 계산 |
| ✅ | 120860 | 직사각형 넓이 | 좌표 정렬 |
| ✅ | 120864 | 숨어있는 숫자 (2) | re.findall |
| ✅ | 120866 | 안전지대 | Set 좌표 추적 |
| ✅ | 120886 | A로 B 만들기 | Counter 아나그램 |
| ✅ | 120888 | 중복된 문자 제거 | dict.fromkeys() |
| ✅ | 120891 | 369게임 | 문자열 카운팅 |
| ⬜ | 181853 | 뒤에서 5등까지 | heapq.nsmallest() |
| ⬜ | 181855 | 문자열 묶기 | Counter |
| ⬜ | 181856 | 배열 비교하기 | cmp 패턴, 튜플 비교 |
| ⬜ | 181861 | 배열의 원소만큼 추가하기 | chain.from_iterable() |
| ⬜ | 181862 | 세 개의 구분자 | re.split() |

---

## 🔄 다음 복습 예정

### 미완료 월간 복습 (5문제)
- [ ] 181853 — 뒤에서 5등까지 (heapq)
- [ ] 181855 — 문자열 묶기 (Counter)
- [ ] 181856 — 배열 비교하기 (cmp 패턴)
- [ ] 181861 — 배열의 원소만큼 추가하기 (chain)
- [ ] 181862 — 세 개의 구분자 (re.split)

---

## 💡 오늘의 깨달음

1. **re.split vs re.findall**: 목적에 맞는 함수 선택이 코드 가독성을 좌우함
2. **역발상의 힘**: 안전지역 직접 세기보다 위험지역 세고 빼기가 더 간단할 수 있음
3. **Boolean 산술**: Python에서 `sum(bool_iterable)`은 True 개수를 세는 관용구
4. **dict 순서 보존**: Python 3.7+에서 dict가 삽입 순서를 보존하는 특성 활용
