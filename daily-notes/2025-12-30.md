# 2025-12-30 학습 노트

## 📝 풀이한 문제 (3문제)

| 문제번호 | 제목 | 핵심 개념 | 복습 |
|---------|------|----------|:----:|
| 181864 | 문자열 바꿔서 찾기 | `str.translate()` + `str.maketrans()` 동시 치환 | |
| 181865 | 간단한 식 계산하기 | `operator` 모듈, 딕셔너리 디스패치 패턴 | 🔁 |
| 181866 | 문자열 잘라서 정렬하기 | `split` + `filter` + `sort` 체이닝 | |

---

## 🎯 핵심 학습 내용

### 1. 문자 동시 치환 패턴 (문제 #181864)

**문제:** A↔B를 서로 바꾸기

**❌ 잘못된 방법: `replace()` 연속 사용**
```python
s = "AB"
s.replace("A", "B").replace("B", "A")  # "AA" - 모두 A가 됨!
```
첫 번째 replace에서 A→B가 되고, 두 번째에서 모든 B→A가 되어버림.

**✅ 올바른 방법: `translate()` + `maketrans()`**
```python
s = "AB"
s.translate(str.maketrans("AB", "BA"))  # "BA" - 동시에 치환!
```

**작동 원리:**
1. `str.maketrans("AB", "BA")` → `{65: 66, 66: 65}` (ASCII 코드 매핑 테이블)
2. `translate()`가 한 번의 패스로 모든 문자를 매핑에 따라 치환

**활용 예시:**
```python
# 대소문자 스왑
s.translate(str.maketrans("abcABC", "ABCabc"))

# ROT13 암호화
import string
rot13 = str.maketrans(
    string.ascii_lowercase + string.ascii_uppercase,
    string.ascii_lowercase[13:] + string.ascii_lowercase[:13] +
    string.ascii_uppercase[13:] + string.ascii_uppercase[:13]
)
"Hello".translate(rot13)  # "Uryyb"
```

---

### 2. operator 모듈과 딕셔너리 디스패치 (문제 #181865)

**문제:** 문자열 `"43 + 12"`를 파싱해서 계산하기

**방법 1: 조건문 분기**
```python
a, op, b = binomial.split()
a, b = int(a), int(b)
if op == "+": return a + b
elif op == "-": return a - b
else: return a * b
```

**방법 2: 딕셔너리 디스패치 + operator 모듈**
```python
import operator

ops = {"+": operator.add, "-": operator.sub, "*": operator.mul}
a, op, b = binomial.split()
return ops[op](int(a), int(b))
```

**왜 operator 모듈이 필요한가?**
```python
op = +  # ❌ SyntaxError - 연산자는 변수에 담을 수 없음
op = operator.add  # ✅ 함수는 변수에 담을 수 있음
```

**딕셔너리 디스패치의 장점:**
- 조건문 없이 깔끔한 코드
- 새 연산자 추가 시 딕셔너리에 한 줄만 추가
- Open-Closed Principle 준수

**주요 operator 함수:**
| 연산자 | 함수 |
|--------|------|
| `+` | `operator.add` |
| `-` | `operator.sub` |
| `*` | `operator.mul` |
| `/` | `operator.truediv` |
| `//` | `operator.floordiv` |

---

### 3. split + filter + sort 체이닝 (문제 #181866)

**문제:** "x"로 문자열을 분할하고, 빈 문자열 제거 후 정렬

**한 줄 솔루션:**
```python
sorted(s for s in my_string.split("x") if s)
```

**단계별 이해:**
```python
"axbxcxdx".split("x")  # ['a', 'b', 'c', 'd', ''] - 빈 문자열 포함!
[s for s in ... if s]  # 빈 문자열 필터링 (''는 falsy)
sorted(...)            # 사전순 정렬
```

**빈 문자열 필터링 방법 비교:**
```python
# 1. 조건 표현식 (가장 Pythonic)
[s for s in lst if s]

# 2. filter(None, ...) - 함수형 스타일
list(filter(None, lst))

# 3. filter(bool, ...) - 명시적
list(filter(bool, lst))
```

**왜 `if s`가 작동하는가?**
- 빈 문자열 `""`는 Python에서 falsy
- `if s`는 `if bool(s)`와 동일
- 빈 시퀀스(`""`, `[]`, `{}`, `()`)는 모두 falsy

---

## 📚 새로 작성한 학습 노트

- `notes/operator-module.md` — operator 모듈 종합 정리
  - 산술/비교/논리 연산자 함수
  - 딕셔너리 디스패치 패턴
  - `itemgetter`, `attrgetter` 활용
  - `reduce`와 조합 예시

---

## 💡 오늘의 깨달음

1. **동시 치환**: 여러 문자를 서로 바꿀 때는 `translate()` + `maketrans()` 필수
2. **연산자를 값으로**: `operator` 모듈로 연산자를 함수처럼 다룰 수 있음
3. **딕셔너리 디스패치**: 조건문을 딕셔너리 매핑으로 대체하면 확장성 향상
4. **falsy 활용**: 빈 문자열/빈 리스트의 falsy 특성을 필터링에 활용

---

## 🔄 다음에 복습할 것

- [ ] 181865 — operator 모듈 활용, 딕셔너리 디스패치 패턴 익숙해지기
