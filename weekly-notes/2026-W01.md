# 2026년 1주차 (W01) 주간 복습

> 복습 기간: 2025-12-31 ~ 2026-01-01
> 작성 일자: 2026-01-01

---

## 📊 복습 결과 요약

| 결과 | 문제 수 | 비율 |
|------|---------|------|
| ✅ 완벽 | 6 | 55% |
| 🔄 재복습 필요 | 5 | 45% |
| **총계** | **11** | 100% |

---

## 📋 복습 문제 상세

### 2025-12-31 (1문제 풀이 + 1문제 복습)

| 문제번호 | 제목 | 핵심 개념 | 결과 |
|----------|------|-----------|:----:|
| 181867 | x 사이의 개수 | `split()` 빈 문자열 보존 | ✅ |
| 181832 | 정수를 나선형으로 배치하기 | 4방향 벡터, Spiral Matrix | ✅ |

### 2026-01-01 (10문제 복습)

| 문제번호 | 제목 | 핵심 개념 | 결과 |
|----------|------|-----------|:----:|
| 181844 | 배열의 원소 삭제하기 | Set 변환 O(1) 조회 | ✅ |
| 181847 | 0 떼기 | `lstrip()` | ✅ |
| 181853 | 뒤에서 5등까지 | `heapq.nsmallest()` | 🔄 |
| 181855 | 문자열 묶기 | `Counter` | 🔄 |
| 181856 | 배열 비교하기 | cmp 패턴, 튜플 비교 | 🔄 |
| 181857 | 배열의 길이를 2의 거듭제곱으로 만들기 | `bit_length()` | ✅ |
| 181858 | 무작위로 K개의 수 뽑기 | `dict.fromkeys()` | ✅ |
| 181861 | 배열의 원소만큼 추가하기 | `chain.from_iterable()` | 🔄 |
| 181862 | 세 개의 구분자 | `re.split()` | 🔄 |
| 181865 | 간단한 식 계산하기 | `operator` 모듈 | ✅ |

---

## 💡 이번 주 핵심 패턴 정리

### 1. cmp 패턴 — 비교 결과를 -1, 0, 1로 변환

```python
# Python 2의 cmp() 대체 패턴
(a > b) - (a < b)

# 동작 원리:
# a > b → 1 - 0 = 1
# a < b → 0 - 1 = -1
# a == b → 0 - 0 = 0

# 활용: 배열 비교
def compare(arr1, arr2):
    t1, t2 = (len(arr1), sum(arr1)), (len(arr2), sum(arr2))
    return (t1 > t2) - (t1 < t2)
```

### 2. 튜플 비교 — 다중 기준 정렬

```python
# Python 튜플은 사전순(lexicographic) 비교
(3, 10) > (2, 999)  # True (첫 번째로 결정)
(3, 10) > (3, 5)    # True (두 번째로 결정)

# 활용: "길이 우선, 같으면 합 비교"
(len(arr1), sum(arr1)) > (len(arr2), sum(arr2))
```

### 3. Set 조회 최적화

```python
# ❌ O(n × m)
[x for x in arr if x not in delete_list]

# ✅ O(n + m)
delete_set = set(delete_list)
[x for x in arr if x not in delete_set]
```

### 4. heapq vs sorted — Top-K 문제

| 방식 | Time | Space | 언제 유리? |
|------|------|-------|----------|
| `sorted(lst)[:k]` | O(n log n) | O(n) | n 작거나 k ≈ n |
| `heapq.nsmallest(k, lst)` | O(n log k) | O(k) | k << n |

### 5. Counter — 빈도 카운팅

```python
# 길이별 문자열 개수
from collections import Counter
max(Counter(len(s) for s in strArr).values())
```

### 6. bit_length() — 2의 거듭제곱

```python
# n 이상인 최소 2의 거듭제곱
target = 1 << (len(arr) - 1).bit_length() if len(arr) > 1 else 1

# 예: len=5 → (4).bit_length()=3 → 1<<3=8
```

### 7. dict.fromkeys() — 순서 유지 중복 제거

```python
# set()은 순서 보장 안 함
list(set(arr))  # ❌ 순서 무작위

# dict.fromkeys()는 삽입 순서 보장 (Python 3.7+)
list(dict.fromkeys(arr))  # ✅ [3, 1, 2]
```

### 8. chain.from_iterable() — 리스트 평탄화

```python
from itertools import chain

# 중첩 리스트 평탄화
list(chain.from_iterable([i] * i for i in arr))

# ❌ sum()은 O(n²) - 비효율적!
sum([[i] * i for i in arr], [])
```

### 9. re.split() — 다중 구분자 분할

```python
import re

# a, b, c 모두를 구분자로 사용
re.split(r"[abc]", "baconlettucetomato")
# → ['', '', 'onlettu', 'etom', 'to']

# 빈 문자열 필터링
[s for s in re.split(r"[abc]", myStr) if s]
```

### 10. operator 모듈 — 연산자 함수화

```python
import operator

op_dict = {"+": operator.add, "-": operator.sub, "*": operator.mul}

a, op, b = "43 + 12".split()
op_dict[op](int(a), int(b))  # 55

# eval()보다 안전!
```

### 11. split() 동작 특성

```python
# 구분자가 연속되면 빈 문자열 포함
"axxb".split("x")  # ['a', '', 'b']

# 시작/끝에 구분자 → 빈 문자열 발생
"xabcx".split("x")  # ['', 'abc', '']

# split() vs split(sep)
"  a  b  ".split()     # ['a', 'b'] - 공백 정리
"  a  b  ".split(" ")  # ['', '', 'a', '', 'b', '', '']
```

### 12. 4방향 벡터 패턴 (Spiral Matrix)

```python
directions = [(0, 1), (1, 0), (0, -1), (-1, 0)]  # 우, 하, 좌, 상
d = 0  # 현재 방향

for i in range(1, n**2 + 1):
    mat[y][x] = i
    ny, nx = y + directions[d][0], x + directions[d][1]

    # 경계 체크 또는 이미 방문 → 방향 전환
    if not (0 <= ny < n and 0 <= nx < n) or mat[ny][nx]:
        d = (d + 1) % 4
        ny, nx = y + directions[d][0], x + directions[d][1]

    y, x = ny, nx
```

---

## 📊 패턴 분류표

| 카테고리 | 패턴 | 코드 |
|----------|------|------|
| **비교** | cmp 패턴 | `(a > b) - (a < b)` |
| | 튜플 비교 | `(len, sum)` |
| **조회** | Set 최적화 | `set(list)` |
| **카운팅** | Counter | `Counter(iterable)` |
| **Top-K** | heapq | `nsmallest(k, lst)` |
| **중복 제거** | dict.fromkeys | `list(dict.fromkeys(arr))` |
| **비트** | bit_length | `n.bit_length()` |
| **평탄화** | chain | `chain.from_iterable(...)` |
| **분할** | re.split | `re.split(r"[...]", s)` |
| **연산자** | operator | `operator.add(a, b)` |
| **2D 탐색** | 4방향 벡터 | `directions = [(0,1), (1,0), (0,-1), (-1,0)]` |

---

## 🔄 다음 주 복습 예정

### 재복습 필요 (이번 주 표시)
- [ ] 181853 — 뒤에서 5등까지 (heapq)
- [ ] 181855 — 문자열 묶기 (Counter)
- [ ] 181856 — 배열 비교하기 (cmp, 튜플 비교)
- [ ] 181861 — 배열의 원소만큼 추가하기 (chain)
- [ ] 181862 — 세 개의 구분자 (re.split)

### 이월된 문제 (W52)
- [ ] 120886 — Counter 아나그램
- [ ] 120888 — dict.fromkeys()
- [ ] 120891 — 369게임 0 처리

---

## 📚 관련 문서

- [daily-notes/2025-12-31.md](../daily-notes/2025-12-31.md) - 12/31 학습 노트
- [daily-notes/2026-01-01.md](../daily-notes/2026-01-01.md) - 01/01 학습 노트
